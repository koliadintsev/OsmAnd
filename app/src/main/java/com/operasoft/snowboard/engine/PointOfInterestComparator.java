package com.operasoft.snowboard.engine;

import java.util.Comparator;

import android.graphics.PointF;

public class PointOfInterestComparator implements Comparator<PointOfInterest> {

	public PointOfInterestComparator() {
		// TODO Auto-generated constructor stub
	}

	@Override
	public int compare(PointOfInterest poi1, PointOfInterest poi2) {
//		double longitude = poi1.getCentroid();
//		double latitude = poi2.getCentroid();
//		
//		if (centroid1.x < centroid2.x) {
//			return -1;
//		} else if (centroid1.x > centroid2.x) {
//			return 1;
//		} else if (centroid1.y < centroid2.y) {
//			return -1;
//		} else if (centroid1.y > centroid2.y) {
//			return 1;
//		}
		
		return 0;
	}

}
